name: pocket_erp
description: Make Magent Great Again

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  qr_code_scanner: ^0.3.0
  vibration: ^1.4.0
  mask_text_input_formatter: ^1.2.1
dev_dependencies:
  flutter_test:
   sdk: flutter
flutter:

  uses-material-design: true